# 项目记忆

## 2026-08-10 - 立项

- 问题：Codex 依赖 CC Switch 的 `127.0.0.1:15721`；退出 CC Switch 会中断请求。
- 证据：早期上游 Responses 直连曾出现 HTTP 503，桌面切换工具的熔断并非纯界面误判。
- 决策：设计独立、本机常驻、仅服务 Codex Responses 的故障转移代理；认证信息只保存在本机，不记录或展示。
- 经验：HTTP 200 的 SSE 流仍可能承载 `response.failed`，必须在提交下游前检查初始事件。

## 2026-08-10 - 本地域名与进程策略

- 决策：公开版本统一使用本机回环地址的 `15722` 端口，不依赖自定义 hosts 记录，也不绑定特权端口 80。
- 失败方案：CC Switch 使用 KeepAlive 会在退出后反复激活窗口并抢占焦点，已停用。
- 最终方案：新代理使用无窗口 Background LaunchAgent，仅异常退出时重启。

## 2026-08-10 - 首次迁移

- 结果：本机 LaunchAgent 已运行，客户端 `fox.base_url` 已从 `15721` 切换到 `127.0.0.1:15722/v1`。
- 保留：未修改 `state_5.sqlite`、会话 JSONL、项目目录或认证文件；切换后当前会话继续工作。
- 回滚点：`runtime/backups/config.toml.20260810-102805-before-proxy-switch`。
- 公开化调整：取消自定义 hosts 依赖，默认回环地址可直接使用。

## 2026-08-10 - 可观测性

- 新增：当前 Provider 行高亮并标记“使用中”；最近 100 条事件在页面展示，磁盘最多保留 200 条。
- 事件字段：时间、Provider、成功/失败、耗时、脱敏信息。
- 隐私：事件日志不记录提示词、响应正文或密钥。

## 2026-08-10 - 动态 Provider 管理

- 问题：原实现把五个 Provider 写死，且认证信息分散，无法在管理页安全增删改。
- 尝试：直接改原混合文件会影响无关凭据，因此未采用。
- 最终方案：增加本机 API 与弹窗完成添加、改名、换密钥和删除；状态、事件、日志和 `dist` 均不含完整密钥。
- 经验：删除后的动态列表必须以持久化配置和当前 key 文件为准，不能在重启时重新注入历史硬编码默认项。

## 2026-08-10 - 启动与安装韧性

- 问题：LaunchAgent 自动拉起与 `bootstrap` 可能发生竞态，出现服务已运行但安装脚本报错；固定操作列也导致中文按钮换行。
- 最终方案：安装流程轮询卸载和健康状态，校验服务身份及有效渠道；失败自动恢复上一份 `dist` 和 plist。增加 `doctor` 自检与 `repair` 修复入口。
- 容错：持久化数字字段损坏时回退默认值，不因单个错误字段阻止服务启动；key 和状态继续原子写入；无窗口 LaunchAgent 始终保活。
- UI：Provider 列宽按内容自适应，操作按钮禁止换行，并保留窄屏布局降级。

## 2026-08-10 - 品牌与中英双语

- 决策：最终产品名采用 `Switch Local Proxy`，计划 GitHub 仓库名为 `switch-local-proxy`；2026-08-10 通过 GitHub Search API 检查时完全同名结果为 0。名称不绑定 Codex、Claude 或具体模型厂商。
- 语言：支持 `English` 与 `简体中文`；跟随系统时中文系统显示简体中文，其他系统回退英文，手动选择持久化并覆盖系统判断。
- 翻译：使用授权的 Tongyi Qwen / DashScope 生成 67 项英文基线，随后按运维界面语境校正文案；新增请求成功文案后共 68 项。
- 验证：构建必须比较中英 key 集与占位符，并保持 `Switch Local Proxy`、`English`、`简体中文` 等固定名称。
- 发布结果：代码已发布到 `heqing7840/switch-local-proxy`，许可证为 The Unlicense；公开内容已检查并排除本机 Key、私有上游地址和运行时数据。

## 2026-08-10 - 请求记录分页

- 问题：最近请求记录持续增长后难以浏览，也无法控制单页密度。
- 最终方案：状态接口返回保留的最近 200 条脱敏事件；前端提供 10/20/50/100 条分页大小、页码摘要和前后翻页。
- 持久化：每页数量保存在浏览器 `switchLocalProxy.eventPageSize`，当前页只属于本次页面会话。
- 隐私：分页只处理现有事件元数据，仍不记录或传输提示词、响应正文和密钥。

## 2026-08-10 - 模型与密钥可观测性

- 问题：Provider 名称不能证明请求使用的是 `sol` 还是 `luna`，编辑渠道时也无法核对当前 key。
- 最终方案：覆盖上游模型前保存客户端模型；事件记录 `requested_model` 和实际 `model`。状态接口新增固定格式 `key_hint`，队列和编辑弹窗复用同一摘要。
- 安全边界：完整 key 仍只在服务端按请求读取，浏览器、状态文件、事件文件、日志和 `dist` 不保存完整密钥；空密钥编辑仍保持原值。

## 2026-08-10 - 上下文能力被动观测

- 证据：供应商记录显示 P3 成功处理 364,127 输入 token、P5 成功处理 349,395、P4 成功处理 325,945；不能据此认定 P3/P4 有较低固定窗口。
- 决策：不执行每日主动超长探测；从真实请求开始记录 request ID、输入 token 估算、上下文错误样本和每条线路最大成功长度。
- 结果：状态与请求历史新增观测字段，未来可基于同一故障转移链判断线路异常，而不把单次上下文问题直接当成永久容量上限。

## 2026-08-10 - 502 同 Key 短重试

- 问题：供应商确认部分 502 与会话上下文或瞬时上游链路有关；立即切 Key 会增加线路切换，并可能降低供应商侧缓存命中。
- 决策：首次 HTTP 502 不立即熔断，等待 1 秒后在同一 Key 重试 1 次；仍失败才记一次失败、冷却并切换。其他故障保持原逻辑。
- 验证：端到端测试模拟首次 502、第二次 200，确认共请求同一上游两次、最终成功、失败计数为 0、事件 `retry_count=1`。
- 取舍：不做无限或多次重试，避免上下文确定性错误带来额外等待和重复计费；SSE 已输出有效内容后仍禁止重试。
- 修正：输入 token 估算跳过内嵌图片和文件的 base64 正文，防止附件体积被误当成文本上下文。
- 迁移：估算器版本升级时清空旧算法生成的约 145 万 token 最大值，从新请求重新累计；其他运行统计和配置保持不变。
- 兼容：旧事件保留成功/失败等证据，但因没有估算器版本，页面将其输入估算显示为 `-`，不与新样本混用。

## 2026-08-10 - 公共领域发布

- 需求：允许他人无需授权或署名即可复制、修改、商用、闭源和重新发布，尽量接近放弃版权。
- 决策：采用 `The Unlicense`，而不是仍要求保留许可与通知的 Apache 2.0；项目根目录维护标准 `LICENSE` 文本。
- 边界：公共领域奉献和免责声明以 `LICENSE` 原文为准；不同司法辖区对版权放弃的承认可能不同，本项目不作额外专利或商标授权承诺。

## 2026-08-10 - Claude Code / Anthropic Messages 适配

- 需求：让项目不只服务 Codex，也能作为 Claude Code 的本地多 Key 故障转移代理。
- 官方依据：Claude Code 通过 `ANTHROPIC_BASE_URL` 和网关凭据连接 LLM Gateway；Messages API 使用 `/v1/messages`、`x-api-key` 和 `anthropic-version`。
- 最终方案：新增 `anthropic_messages` 适配器，支持 Messages 与 Count Tokens；移除客户端占位认证并注入 Provider Key，保留 Anthropic 协议头，复用现有重试、冷却、SSE 预检和事件记录。
- 兼容边界：这是 Anthropic 协议透传，不是 Anthropic 与 OpenAI 的请求/响应转换。上游不支持 Messages 时，代理不能凭空让 Claude Code 工作。
- 安全：Claude Code 可使用 `ANTHROPIC_AUTH_TOKEN=local-proxy` 作为非敏感本地占位值，真实 Provider 密钥仍只存在于被忽略的本机数据库。

## 2026-08-10 - 公共发布隐私与本机接口加固

- 问题：只扫描当前 `dist/` 和跟踪文件不足以证明公开仓库安全；Git 历史、未忽略的新文件、运行快照及本机跨站请求都可能造成泄漏或滥用。
- 验证：安全审查复现了跨站 `text/plain` 请求可调用本机接口、Cookie 可被转发、上游错误可持久化凭据占位值，以及 SSE 文本误判；界面动态值转义检查通过。
- 最终结果：增加 Origin 校验、16 MiB 请求上限、30 秒读取超时、Cookie 隔离、错误脱敏和结构化 SSE 判断；`privacy-check` 扫描工作区、Git 全历史和 `dist/` 白名单，且不输出命中原文。
- 发布规则：`./run.sh verify` 必须包含隐私扫描；任何命中都阻止公开推送，不能通过扩大忽略范围绕过真实跟踪或历史泄漏。

## 2026-08-11 - Provider 协议范围提示

- 问题：只显示“渠道名称”和 API Key 容易让使用者把 Provider 误认为支持所有模型或 API 协议。
- 最终结果：添加/编辑弹窗固定标注当前仅支持 GPT/Codex 的 OpenAI Responses 和 Claude 的 Anthropic Messages；Provider 不按客户端类型绑定，请求路径选择适配器。

## 2026-08-11 - 本机 Host 别名的 Origin 校验

- 问题：管理页通过 `p.a.com:15722` 这类本机 hosts 别名访问时，严格的 loopback 主机名白名单会把正常的渠道开关请求误判为跨站请求并返回 403。
- 最终结果：带 `Origin` 的请求除标准 loopback 主机名外，也允许与请求 Host 相同的本机别名，端口仍必须匹配本地服务端口；外部 Origin 保持拒绝。

## 2026-08-11 - 上下文错误不应熔断 Key

- 问题：供应商把上下文窗口错误包装为 HTTP 200 的 JSON/SSE 错误时，代理会把请求级问题当成 Provider 故障，触发冷却并增加切换等待。
- 最终结果：上下文错误继续允许尝试其它 Provider，但不设置冷却；全部候选都是上下文错误时返回 `400 context_length_exceeded`，其它 502/503/超时仍按故障转移处理。

## 2026-08-11 - OpenAI Chat Completions 兼容适配

- 需求：让同一份本地代理可以接入 Gemini 的 OpenAI 兼容入口，以及 Qwen、DeepSeek、Mistral 等常见兼容服务；同时保留 Grok 的 Responses 入口和 Claude 的 Messages 入口。
- 最终结果：新增 `POST /v1/chat/completions` 适配器，复用现有认证、模型覆盖、重试、冷却、SSE 预检和事件记录；Chat Completions 流式首段通过 `choices[].delta/message/finish_reason` 判定已产生有效输出。
- 边界：这是协议透传，不是格式转换；原生 Gemini `generateContent` 不支持。当前配置仍只有一个上游基地址和强制模型，若要同时配置不同供应商的不同上游，需要后续增加按 Provider 的上游配置。

## 2026-08-11 - LaunchAgent bootstrap 兼容回退

- 问题：本机图形登录域的 `launchctl bootstrap` 返回 `Input/output error`，导致安装脚本在构建成功后报安装失败，服务未被加载。
- 验证：相同 plist 通过 `launchctl load -w` 成功加载，服务监听本机端口且 `doctor` 健康检查通过；因此不是 Python 进程、端口或渠道配置失败。
- 最终结果：`run.sh install`、`repair` 和 `start` 共用加载函数。优先 `bootstrap`，若服务未加载再回退 `load -w`，随后必须通过原有健康检查才报告成功。

## 2026-08-11 - CC Switch 本地后备无需 API Key

- 问题：若 CC Switch 的本地 Proxy Provider 要求填写 API Key，用户容易误把真实上游密钥填入客户端配置，或因为空值导致切换失败。
- 证据：代理端不校验客户端 `Authorization`，现有无认证请求测试仍能进入路由；真实 Provider 密钥始终由代理从本机数据库注入。
- 最终结果：CC Switch 的 Switch Local Proxy 条目使用 `requires_openai_auth = false` 与空认证对象；UI 中 API Key 可保持空白。该条目仅用于手动切换，不加入 CC Switch 自动故障转移队列。

## 2026-08-11 - 本地版本更新提示

- 问题：用户从 GitHub 安装后无法知道本机版本是否落后，手动反复查看仓库既麻烦也容易遗漏更新。
- 最终方案：公开仓库根目录维护 `version.json`；管理页打开时调用本机 `/api/update`，后台低频读取该公开文件。成功缓存 24 小时，网络失败仅缓存 15 分钟。
- 隐私与可用性：请求没有认证、Cookie、渠道、Key、私有上游地址、使用记录或设备标识；检查故障只显示暂不可用，不进入故障转移链，也不影响代理请求。
- 升级路径：用户查看公开提交记录后运行 `git pull --ff-only && ./run.sh repair`；既有安装健康检查与回滚机制继续生效。

## 2026-08-11 - SQLite 与独立上游渠道

- 需求：同一故障转移队列可混用不同中继商或官方渠道，同时不把任何用户的地址、密钥、状态或请求记录发布到 GitHub。
- 最终方案：渠道在 SQLite 中保存独立 API 前缀、模型和协议；路由按渠道重写基址并保留本地 `/v1` 后的协议路径。
- 存储边界：密钥、渠道配置、运行状态和请求历史统一写入项目忽略的 `runtime/proxy.sqlite3`；公开源码只包含通用 UI、迁移逻辑和安全扫描规则。

## 2026-08-11 - 管理弹窗与状态轮询隔离

- 问题：管理页每 3 秒刷新 Provider 队列；原生模态表单在部分浏览器环境中会被相邻 DOM 重绘影响，导致添加或编辑渠道时窗口意外关闭。
- 最终结果：渠道表单打开期间暂停状态轮询渲染；取消后立即拉取一次最新状态，保存和删除则直接使用接口返回的最新状态渲染。
- 取舍：表单打开的短时间内队列状态不自动变化，换来私有渠道信息输入不被中断。

## 2026-08-11 - 来源策略与 GPT Luna 防护

- 需求：允许本机、局域网、所有来源或指定 IP/CIDR 使用代理；全局策略作为默认值，单渠道设置优先。指定范围一行一条规则。
- 安全边界：模型端口监听本机网卡，管理页面和管理 API 始终只接受 loopback；模型请求在读取正文前按渠道有效来源策略校验。默认全局仅本机，渠道默认继承。
- 模型决策：渠道表单不接受手输模型名。内置 GPT 防护把 `gpt-*` 请求映射到本机全局 GPT 模型，非 GPT 家族不受影响；支持 Luna 的渠道可选择客户端模型策略。
- 可观测性：状态页显示当前本机 IPv4 连接地址、渠道来源策略，以及客户端模型与实际模型的差异。发现新版本时右上角显示项目内红色 SVG 更新图标。
- 前端细节：IP 规则区域通过强制 `[hidden]` 规则只在 CIDR 模式显示；API Key 紧跟 API 地址。连接地址内容不变时不参与轮询重绘，并提供独立复制按钮，避免文本选择每 3 秒丢失。

## 2026-08-11 - 编辑渠道地址与上游 401 诊断

- 问题：渠道状态只显示是否有独立上游地址，编辑表单因此无法回显已保存地址；新渠道 `ar` 在管理页看似存在，但上游请求失败。
- 尝试：读取本机 SQLite 配置并对 `ar` 的模型列表与最小 Responses 请求做诊断，不输出密钥。
- 结果：增加 loopback-only `GET /api/provider?name=...`，编辑时回显 URL；`ar` 已启用且无冷却、历史有成功请求，但当前直接上游均返回 `401 unauthorized client detected`。
- 结论：该 401 应先由中继服务商核对密钥状态、来源 IP 或客户端识别规则；确认前不要把 `ar` 提升为首选渠道。

## 2026-08-11 - 服务假性断线与 SQLite 文件描述符泄漏

- 问题：服务运行一段时间后端口或管理页失效，用户感觉后台服务经常断掉。
- 证据：`service.stderr.log` 出现大量 `OSError: [Errno 24] Too many open files`；新进程短时间内对同一 SQLite 文件积累近百个打开描述符。LaunchAgent 本身已经启用 `RunAtLoad` 与 `KeepAlive`，进程没有频繁崩溃记录。
- 根因：`sqlite3.Connection` 的 `with` 只提交或回滚事务，不会关闭连接；每次状态刷新和事件写入都遗留一个数据库文件描述符。
- 最终结果：数据库初始化、读取、写入统一使用 `contextlib.closing`，写操作保留事务上下文；增加连续 20 轮读写并核对 40 个连接全部关闭的回归测试。`doctor` 同时校验登录自动启动和异常退出自动重启。
- 结论：先消除资源泄漏，再依赖现有无窗口 LaunchAgent 自愈；不引入可能抢焦点的桌面应用守护方式。

## 2026-08-12 - 登录自启与独立健康守护

- 问题：用户重启电脑后仍习惯手动运行 `./run.sh start`，需要覆盖启动项被卸载、主进程停止和进程存活但健康接口失效三类情况。
- 证据：本次开机 6 分钟后主 LaunchAgent 已显示 `runatload | keepalive` 且服务健康，说明基础登录自启有效；但单一 KeepAlive 只关注进程生命周期，不验证业务健康。
- 最终方案：新增 `com.switch-local-proxy.watchdog` 无窗口 LaunchAgent，登录即运行并每 15 秒访问本机健康接口；连续两次失败后启用、加载并强制拉起主服务。`./run.sh stop` 先停用守护器，保留可控停机语义。
- 修正：首次安装验证发现 zsh watchdog 在 launchd 中以 127 退出；改用主服务相同的 Python 解释器运行 `dist/support/watchdog.py`，并让 `doctor` 拒绝最近一次非零退出，避免“已加载但未工作”的假健康。
- 验收：重新执行完整安装后，42 项单元测试、语言资源校验和隐私扫描均通过；主动 `bootout` 主服务后，watchdog 在 13.2 秒内重新 bootstrap 并恢复健康，最近退出码为 0。
- 安全边界：守护脚本只访问 `127.0.0.1` 健康接口和操作固定 launchd 标签，不读取或输出渠道地址、请求记录、数据库或 API Key。

## 2026-08-12 - 项目与运行目录完整迁移

- 需求：将源码、Git 历史、未提交改动、`dist`、本机运行数据库、日志、LaunchAgent 运行目录及相关 Codex 会话统一迁到新的项目根目录 `switch-local-proxy`。
- 最终方案：新目录成为唯一项目根目录；重新执行安装以生成指向新路径的主服务和 watchdog plist，旧 `Documents/New project/codex-key-proxy` 路径不再保留。
- 会话范围：`019ff3b5-4f76-7c90-b317-a48ccdf296bb` 与 `019fd9f0-65b7-7623-b758-213c165b4407` 的项目工作目录同步改为新根目录，保留原会话历史。
- 安全边界：迁移保持 `runtime/proxy.sqlite3` 原样，不读取、输出或复制其中的密钥到文档和 Git 跟踪文件。
