# 实施计划

**最后更新：** 2026-08-11
**AI：** Codex

1. 已完成：问题证据收集、功能/技术需求整理与 v0.1 需求确认。
2. 已完成：代理接口、状态机、SSE 初始错误识别、本地管理页及核心测试。
3. 已完成：构建 `dist` 产物、核心测试、迁移脚本、当前 Provider 高亮和持久化请求记录界面。
4. 已完成：安装并启动 LaunchAgent；Codex 已切换到 `http://127.0.0.1:15722/v1`，原会话与项目记录保持不变。
5. 已完成：新增 Provider 添加、修改、删除能力；密钥只保存到项目已忽略的本机数据库，状态接口不回显密钥。
6. 已完成：操作列自适应宽度且按钮文字不换行；增加 `doctor`、`repair`、启动配置容错与安装失败自动回滚。
7. 已完成：项目最终命名为 Switch Local Proxy；增加英文/简体中文资源、系统语言检测、英文回退与持久化手动切换。
8. 已完成：最近请求记录增加分页、每页 10/20/50/100 条选择和本地持久化，状态接口提供全部 200 条保留事件。
9. 已完成：请求记录显示客户端请求模型与实际上游模型；Provider 队列和编辑弹窗显示前6位、后4位的 key 脱敏摘要。
10. 已完成：请求事件开始记录 request ID、输入 token 估算和上下文观测边界，Provider 状态显示历史最大成功输入；不执行主动每日探测。
11. 已完成：HTTP 502 在同一 Provider 上等待 1 秒重试 1 次，仍失败才冷却切换；事件显示重试次数，并排除内嵌图片对文本 token 估算的放大。
12. 已完成：GitHub 仓库名确定为 `switch-local-proxy`，增加 The Unlicense、英文仓库描述并发布到公开仓库。
13. 已完成：公开版本统一使用本机回环地址，不要求修改 `/etc/hosts`。
14. 已完成：增加 Anthropic Messages 透传适配，支持 Claude Code 的 `/v1/messages`、`/v1/messages/count_tokens`、认证替换、模型覆盖、SSE 错误识别和协议标记。
15. 已完成：发布前隐私安全加固，包括本机 Origin 校验、请求大小/超时限制、Cookie 隔离、错误脱敏、精确 SSE 解析和 Git 全历史隐私扫描。
16. 已完成：添加/编辑渠道弹窗标注协议范围，仅支持 GPT/Codex 的 OpenAI Responses 与 Claude 的 Anthropic Messages，不把 Provider 误导为通用模型渠道。
17. 已完成：Origin 校验兼容当前本机 Host 别名，修复 `p.a.com:15722` 管理页渠道启停被 403 拒绝的问题。
18. 已完成：上下文窗口错误不再熔断 Key；所有候选均上下文不足时返回明确的 400。
19. 已完成：增加 OpenAI Chat Completions 透传适配；支持 Gemini 的 OpenAI 兼容入口，以及 Qwen、DeepSeek、Mistral 等兼容服务，并在记录中标注协议。
20. 风险：P1-P4 当前真实返回 503；实现后会被正确冷却，但不能保证它们立即可用。
21. 已完成：CC Switch 的 Switch Local Proxy 手动后备条目改为无客户端认证；API Key 可留空，真实上游密钥仅保存在本机代理数据库。
22. 已完成：管理页增加隐私保护的版本更新提示；仅低频读取公开版本文件，成功每日检查、失败 15 分钟重试，且不影响代理流量。
23. 已完成：渠道扩展为独立 API 地址、模型和协议，并把渠道密钥、配置、运行状态和请求记录统一迁移到本机 SQLite；发布包不包含本机运行数据。
24. 已完成：渠道添加/编辑弹窗打开期间暂停状态轮询渲染，避免浏览器重绘导致表单意外关闭；关闭后立即刷新状态。
25. 已完成：移除渠道模型输入；增加 GPT Luna 防护与渠道“使用客户端模型”覆盖策略，避免人工模型名错误。
26. 已完成：增加全局与渠道两级来源访问策略，支持本机、局域网、所有来源和逐行 IP/CIDR；管理接口保持仅本机。
27. 已完成：状态页显示本机 IPv4 连接地址；发现新版本时右上角显示红色 SVG 更新图标。
28. 已完成：修复本机地址轮询重绘导致无法选择复制；增加逐项复制按钮，并修正 CIDR 输入框条件显示与渠道表单字段顺序。
